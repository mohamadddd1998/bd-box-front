import { FiUser } from "react-icons/fi";

const User = () => {
  return (
    <div className="border-2 border-grayColor p-1.5 rounded-md cursor-pointer">
      <FiUser className="text-grayColor" size={18} />
    </div>
  );
};
export default User;
